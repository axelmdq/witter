<% @tweets.each do |tweet| %>
    <p>
      <%= tweet.text %>
      <%= link_to "Editar", edit_tweet_path %>
      <%= link_to "Eliminar", tweet, :method => :delete, :confirm => "Estas seguro?" %>
    </p>
<% end %>
<% if @tweets.nil? %>
    <%=  "No hay tweets para mostrar, podrías empezar a escribir algunos!"%>
    <%= link_to "Primer tweet", new_tweet_path %>
<% end %>